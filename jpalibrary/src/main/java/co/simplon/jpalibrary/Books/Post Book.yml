info:
  name: Post Book
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/books
  body:
    type: json
    data: |-
      {
        "title" : "Voyage au bout de la terre",
        "description" : "Evasion",
        "available" : true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
